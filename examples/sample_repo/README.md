# Sample Repo (for ECP)

This is a tiny repository used to demonstrate the ECP v1.0 reference implementation.

- `auth.py`: authentication and session handling
- `db.py`: database connection and queries
- `main.py`: example entrypoint wiring auth + db
